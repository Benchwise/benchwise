# Core dependencies (required for basic functionality)
pydantic>=2.0.0
httpx>=0.24.0
numpy>=1.24.0
pandas>=2.0.0
requests>=2.28.0

# Testing dependencies (required for running tests)
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Basic metrics dependencies (recommended)
rouge-score>=0.1.2
sacrebleu>=2.3.0
nltk>=3.8.0

# Note: Install optional dependencies as needed:
# - For LLM APIs: pip install openai anthropic google-generativeai
# - For advanced metrics: pip install bert-score sentence-transformers
# - For fuzzy matching: pip install fuzzywuzzy
# - For NLP: pip install spacy
# - For local models: pip install transformers torch
# - For all features: pip install benchwise[all]
